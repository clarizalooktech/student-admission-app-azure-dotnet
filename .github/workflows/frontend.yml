name: Frontend — local only

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'

jobs:
  notice:
    name: Frontend info
    runs-on: ubuntu-latest
    steps:
      - name: Frontend runs locally
        run: |
          echo "Frontend is served locally for the demo"
          echo "Open frontend/index.html in your browser"
          echo "Make sure config.json points to the live backend:"
          echo "https://ca-admission-dev.mangodune-bf8f3f1e.australiaeast.azurecontainerapps.io"